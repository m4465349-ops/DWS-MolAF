# DWS-MolAF
 train(binary).py is the training script for binary classification tasks, train(regression).py is the training script for regression tasks, dynamic weighting mechanism.py implements the dynamic weighting mechanism for feature selection, and model contains the activation function definitions and training models.
